# Database Timezone Shenanigans



## Requirements
* Maven
* Java 21
* A running PostgreSQL instance (anything from a local Docker image to a remote cluster)
  * Connection details can be configured in `main` method of `App.java`
